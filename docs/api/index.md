# API

API reference for the Cellario Scheduler. C# samples live in `docs/api/samples/` and are
embedded into pages with `use{file="api/samples/...#l1-l40" syntax="csharp"}`.

> Starter page. Child folders and topics will be added under `docs/api/` and linked from
> `docs/Summary.md` as content lands.
