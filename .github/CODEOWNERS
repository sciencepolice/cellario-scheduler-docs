# Review gates per section. Replace the placeholder teams/users with real owners.
# See https://docs.github.com/articles/about-code-owners

# Repo-wide default
*                       @sciencepolice

# Per-section owners
/docs/user-guide/       @sciencepolice
/docs/api/              @sciencepolice
/docs/scripting/        @sciencepolice

# Sync config changes warrant extra scrutiny.
# NOTE: these paths must match the real filenames exactly -- the config is the
# dotfile `.archbee.yaml`, and the nav file is capitalized `Summary.md`. A
# CODEOWNERS path that matches nothing fails silently.
/.archbee.yaml          @sciencepolice
/docs/Summary.md        @sciencepolice
